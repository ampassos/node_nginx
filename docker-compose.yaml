version: '3'

services:
  app:
    build:
      context: node
    container_name: app
    networks:
      - node-network
    entrypoint: wait-for db:3306 -t 40 -- node index.js
    volumes:
      - ./node:/usr/src/app
<<<<<<< HEAD
      #- /usr/src/app/node_modules
=======
      - /usr/src/app/node_modules
>>>>>>> ff10cfe774cd25342092ea1dabc9f70a3d26d8fe
    tty: true
    ports: 
      - "3000:3000"
    depends_on:
      - db
    
  db:
    image: mysql:5.7
    container_name: db
    restart: always
    tty: true
    volumes:
      - ./mysql:/var/lib/mysql
    environment:
      - MYSQL_DATABASE=nodedb
      - MYSQL_ROOT_PASSWORD=root
    
    networks:
      - node-network
    
  nginx:
    build:
      context: ./nginx
    container_name: nginx
    restart: always
    networks:
      - node-network
    ports:
      - "8080:80"

networks:
  node-network:
    driver: bridge
